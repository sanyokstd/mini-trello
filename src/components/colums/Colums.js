import {Container} from 'react-bootstrap';

import './Colums.scss';

const Colums = () => {

    return (
        <Container>
            Colums
        </Container>
    );
}
  
export default Colums;